---
tags: [sesion, bitacora]
proyecto: residencial-zona15
fecha: 2026-06-17
---

# Sesión 2026-06-17 — Visor y editor de contrato (TipTap)

## Qué se hizo

Feature completo: visualización inline del PDF del contrato y edición tipo Google Docs con TipTap.

### 1. Migraciones DB

**`supabase/migrations/2026-06-17_contract-template.sql`**
- Crea `contract_template (key TEXT PRIMARY KEY, value JSONB, updated_at TIMESTAMPTZ)`
- RLS habilitado con policy `template_admin_select` (solo admin/super_admin pueden leer)
- Seed: `landlord`, `banks`, `clauses` (14 cláusulas con `on conflict do nothing`)

**`supabase/migrations/2026-06-17_contract-template-body.sql`**
- Seed: `body_json` — contrato completo en formato Prosemirror JSON (TipTap)
- Usa `on conflict do update` para permitir re-ejecución
- Todos los placeholders `{NOMBRE_INQUILINO}`, `{RENTA}`, etc. están en el JSON como texto plano

> [!warning] GRANT requerido
> En proyectos Supabase nuevos hay que ejecutar manualmente:
> ```sql
> grant select, insert, update, delete on contract_template to service_role;
> grant select on contract_template to authenticated;
> ```
> Sin esto el API devuelve `500: permission denied for table contract_template`.

### 2. Dependencias nuevas

```
@tiptap/react  @tiptap/pm  @tiptap/starter-kit
```

### 3. `ContractPDF.tsx` — reescrito

- Acepta `bodyJson: PmNode | null` (Prosemirror JSON) en lugar de template estructurado
- Función `replacePlaceholders(node, vars)` — recorre el árbol JSON y sustituye `{PLACEHOLDER}` en nodos de tipo `text`
- Función `renderPmNode(node, key)` — convierte nodos Prosemirror a elementos `@react-pdf/renderer`
  - Soporta: `doc`, `heading` (H1/H2/H3), `paragraph`, `bulletList`, `orderedList`, `listItem`, `hardBreak`, `text` (con marks `bold` e `italic`)
- Exporta: `replacePlaceholders`, tipos `PmNode`, `Vars`
- Secciones siempre auto-añadidas por el componente (no editables): persona adicional, parqueo, firmas

### 4. API route `/api/contracts/[contractId]/pdf/route.ts` — actualizado

- Fetcha `body_json`, `landlord`, `banks` de `contract_template`
- Construye mapa `vars: Vars` con 21 placeholders
- Llama `replacePlaceholders(bodyJson, vars)` antes de pasar al componente PDF
- Cláusula 7 (`{CLAUSULA_DEPOSITO}`) se resuelve dinámicamente según si hay depósito pagado en `income_extras`

### 5. API route `/api/admin/contract-template/route.ts` — nuevo

- `GET`: devuelve `{ landlord, banks, clauses, body_json }` — requiere admin/super_admin
- `PUT`: recibe `{ key, value }`, hace upsert, escribe en `audit_log`

### 6. `ContractPDFViewer.tsx` — nuevo componente

- Modal grande (95vw × 90vh, z-[60])
- **Modo vista**: iframe apuntando a `/api/contracts/{id}/pdf?t={timestamp}` (cache-bust)
- **Modo edición**: editor TipTap con StarterKit
  - Toolbar: B, I, H1, H2, H3, lista con viñetas, lista numerada, undo, redo
  - Panel inferior con chips de placeholders (clic → copia al portapapeles)
  - Al entrar: fetch `body_json` → `editor.commands.setContent(data.body_json)`
  - Al guardar: `editor.getJSON()` → PUT `body_json` → recarga iframe

### 7. `ContractInfoDialog.tsx` — modificado

- Reemplaza el `<a target="_blank">` por un botón que abre `ContractPDFViewer` inline
- Envuelto en Fragment `<>` para soportar el modal como sibling

### 8. `globals.css` — añadido

- Clase `.tiptap-contract` con estilos serif para el editor (Georgia 14px, headings, párrafos, listas)

### 9. `app/admin/plantilla/page.tsx`

- Redirige a `/admin/rooms` (la página de plantilla estructurada fue descartada)

## Estado al cerrar

- Todo funciona en local
- Migraciones aplicadas en producción (SQL Editor de Supabase)
- GRANTs aplicados en producción
- **Pendiente**: commit y PR a main

## Archivos modificados/creados

- `supabase/migrations/2026-06-17_contract-template.sql` ← nuevo
- `supabase/migrations/2026-06-17_contract-template-body.sql` ← nuevo
- `components/ContractPDF.tsx` ← reescrito (Prosemirror JSON renderer)
- `app/api/contracts/[contractId]/pdf/route.ts` ← reescrito (placeholder substitution)
- `app/api/admin/contract-template/route.ts` ← nuevo
- `components/admin/ContractPDFViewer.tsx` ← nuevo (TipTap editor + iframe viewer)
- `components/admin/ContractInfoDialog.tsx` ← botón Ver PDF → modal
- `app/globals.css` ← clase `.tiptap-contract`
- `app/admin/plantilla/page.tsx` ← redirect a rooms
- `package.json` / `package-lock.json` ← TipTap dependencies

## Relacionado

- [[modelo-datos]] — nueva tabla `contract_template`
- [[arquitectura]] — nueva API route y componente visor/editor

---

# Sesión 2026-06-17 (continuación) — Finanzas reales + anti-fraude comprobantes (PR #13)

## Qué se hizo

### Problema resuelto
El dashboard de finanzas mostraba ingresos **proyectados** (suma de rentas de contratos activos), no lo realmente cobrado. El cliente también reportó fraude de comprobantes (imágenes repetidas de meses anteriores o modificadas).

### 1. Nueva tabla `monthly_payments`

`supabase/migrations/2026-06-17_monthly-payments.sql`

```
id, contract_id, room_id, period_month, amount, source (receipt|manual),
receipt_id (FK nullable), registered_by, registered_at, notes
unique (contract_id, period_month)
```

- RLS: solo admin/super_admin (policy `admin_all_monthly_payments`)
- Índice `idx_payment_receipts_file_hash` en `payment_receipts.file_hash` para fraud detection
- GRANTs aplicados manualmente en SQL Editor de Supabase

### 2. `ReceiptsDialog.tsx` — aprobación registra ingreso + alerta fraude

Al hacer clic en **Aceptar**:
1. Marca el comprobante como `verified: true` (comportamiento anterior)
2. Calcula `amount = contract.monthly_rent + sum(recurring_charges)`
3. Upsert en `monthly_payments` con `source='receipt'`
4. Audit log incluye el monto

**Anti-fraude:** al cargar el diálogo, query cross-tenant:
- Busca todos los `payment_receipts` con el mismo `file_hash` pero distinto `contract_id`
- Si hay coincidencias, muestra alerta roja en el comprobante afectado: "⚠️ Archivo duplicado detectado: [Nombre] · [Mes]"
- No bloquea al inquilino — solo informa al admin

### 3. `FinancesPanel.tsx` — dashboard real vs proyectado

**KPIs nuevos (5 cards, antes eran 4):**
- **Cobrado** (verde) — suma de `monthly_payments` confirmados este período/propiedad
- **Por cobrar** (ámbar) — suma de renta+recurrentes de contratos sin pago este período
- Ingresos variables, Total egresos, Neto del mes (sin cambio)

**Nueva sección "Estado de cobros":**
- Tabla por contrato: muestra ✓ Q{monto} (verde) o "Pendiente Q{esperado}" (ámbar)
- Indica si el pago fue por comprobante o manual

**Ingreso manual:**
- Botón "+ Registrar ingreso manual"
- Select habitación → pre-llena monto con renta + recurrentes del contrato
- Monto editable (para efectivo, ajustes, etc.)
- Si ya existe un pago por comprobante para ese período → advertencia ámbar en primer clic; guarda en segundo

### 4. `lib/supabase/types.ts`

Añadida interface `MonthlyPayment`.

## Estado al cerrar

- Código mergeado a main (PR #13)
- Migración + GRANTs aplicados en producción
- **Pendiente:** validación en producción con datos reales — posibles mejoras tras feedback del cliente

## Archivos modificados/creados

- `supabase/migrations/2026-06-17_monthly-payments.sql` ← nuevo
- `lib/supabase/types.ts` ← `MonthlyPayment` interface
- `components/admin/ReceiptsDialog.tsx` ← ingreso automático al aprobar + alerta fraude
- `components/admin/FinancesPanel.tsx` ← dashboard real, tabla estado, ingreso manual

## Relacionado

- [[modelo-datos]] — nueva tabla `monthly_payments`
- [[arquitectura]] — FinancesPanel y ReceiptsDialog modificados
- [[decisiones/finanzas-ingresos-reales]] — decisiones de diseño (real vs proyectado, anti-fraude)
