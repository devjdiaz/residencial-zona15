---
tags: [bitacora]
fecha: 2026-06-16
---

# Sesión 2026-06-16 — Reforma del contrato (mora, depósito, persona adicional, cláusulas nuevas)

Volver a [[00-Indice]].

## Qué se hizo
Implementación completa (en código, sin aplicar en producción todavía) de los cambios al contrato pedidos por el cliente. Detalle de la decisión y diseño en [[reforma-contrato-2026-06]]:

- Eliminada la cláusula de MORA de `components/ContractPDF.tsx` (único lugar donde existía en todo el proyecto).
- Cláusula de depósito ahora dinámica (muestra monto real desde `income_extras`) + nueva cláusula de no devolución si el inquilino sale antes del plazo pactado.
- Persona adicional: nueva migración `2026-06-16_contract-additional-person.sql` (`contracts.has_additional_person` + 4 columnas de texto). Se reutilizó el checkbox existente "Persona adicional (mensual)" en `ContractDialog.tsx` y `ContractInfoDialog.tsx` para pedir nombre/DPI/teléfono/teléfono alterno (sin correo — nunca tiene credenciales). Se refleja en el PDF en una sección condicional nueva.
- Cancelación anticipada: 15 → 30 días.
- 6 cláusulas nuevas agregadas (mantenimiento/limpieza, huéspedes, no fumar, convivencia, causales de terminación inmediata, impago reiterado, entrega de llaves, garita — ver numeración completa en la nota de decisión).
- `supabase/schema.sql` y `knowledge-vault/modelo-datos.md` actualizados para reflejar las columnas nuevas.

## Estado al cerrar
- `tsc --noEmit` limpio. `eslint` limpio en los 5 archivos tocados (el resto del repo ya tenía errores preexistentes no relacionados, por `react-hooks/set-state-in-effect`).
- Migración **no aplicada en producción** — el usuario la aplicará desde el SQL Editor de Supabase (decisión explícita: no se autorizó a Claude a correrla con el service role key).
- Código sin commit, en `feature/historial-module` — pendiente de autorización.

## Parte 2 (mismo día) — vehículo en el parqueo
Mismo patrón que persona adicional, ahora para el checkbox "Parqueo (mensual)": al marcarlo pide tipo de vehículo (selector cerrado moto/carro), marca, línea, color y placa. Migración `2026-06-16_contract-parking-vehicle.sql` (`contracts.has_parking` + 5 columnas, `parking_vehicle_type` con `check`). Nueva sección condicional en el PDF "VEHÍCULO AUTORIZADO (PARQUEO)", justo después de "PERSONA ADICIONAL AUTORIZADA". Detalle completo en [[reforma-contrato-2026-06]].

- Migración aplicada en producción por el usuario.
- `tsc --noEmit`/`eslint` limpios.
- Verificación con PDF real: generé el PDF de un contrato existente (sin persona adicional ni parqueo) dos veces — antes y después de aplicar cada migración — confirmando 200 OK y que las secciones condicionales no aparecen cuando no aplican. Falta probar en el navegador el caso con persona adicional y/o parqueo activos (formularios + PDF resultante).

## Pendiente / próximos pasos
1. Usuario prueba en el navegador (servidor local en `:3001`, ya corriendo): crear/editar un contrato marcando persona adicional y/o parqueo, confirmar que los sub-formularios validan bien y que el PDF muestra las secciones nuevas correctamente.
2. Commit / PR cuando el usuario autorice (incluye ambas migraciones y todos los cambios de esta sesión).
