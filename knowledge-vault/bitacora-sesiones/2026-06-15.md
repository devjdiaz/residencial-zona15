---
tags: [bitacora]
fecha: 2026-06-15
---

# Sesión 2026-06-15 — Migración a producción + E2E renta por contrato

Volver a [[00-Indice]].

## Qué se hizo
- **Migración aplicada en producción**: `supabase/migrations/2026-06-12_monthly-rent.sql` (columna `monthly_rent` + backfill). Verificado con `select monthly_rent from contracts`.
  - 3 contratos quedaron en `null` tras el backfill: cuartos 4, 5, 6 de El Maestro, sin `type_id` asignado. Son contratos activos. Detalle y acción pendiente en [[renta-por-contrato]].
- **E2E manual** sobre `feature/historial-module`, sin tocar datos de producción (solo lectura + revisión de código):
  - Contrato existente muestra renta mensual correctamente (Q1,600 en cuarto 1).
  - Código de Finanzas y portal del inquilino confirmado leyendo `monthly_rent ?? room_type.price ?? 0`.
  - PDF del contrato (`/api/contracts/{id}/pdf`) seguía dando 404 — encontrados y corregidos 2 bugs nuevos:
    1. `require("@react-pdf/renderer")` fallaba porque el paquete es ESM puro → cambiado a `await import()`.
    2. Embed de `tenant_profiles` ambiguo (dos FK entre `contracts` y `tenant_profiles`) → desambiguado con `tenant_profiles!contracts_tenant_profile_id_fkey(...)`.
    3. Al remover el `require()`, TypeScript dejó de tipar como `any` y expuso un mismatch real de tipos en `renderToBuffer` → resuelto con cast dirigido.
  - PDF confirmado devolviendo 200 con datos correctos tras los fixes.

## Estado al cerrar
- `tsc --noEmit` limpio, `eslint` limpio.
- Migración aplicada en producción (con la salvedad de los 3 contratos pendientes de `type_id`).
- Código sin commit — pendiente de autorización.

## Pendiente / próximos pasos
- Cliente asigna `type_id` a cuartos 4, 5, 6 de El Maestro desde el backoffice → correr de nuevo el `update` del backfill para esos 3.
- Commit / PR cuando el usuario autorice.
