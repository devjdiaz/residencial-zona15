---
tags: [bitacora]
fecha: 2026-06-13
---

# Sesión 2026-06-13 — Renta editable por contrato

Volver a [[00-Indice]].

## Qué se hizo
- 💰 **Renta mensual editable por contrato** (`contracts.monthly_rent`), sobre la rama `feature/historial-module`. Decisión: [[renta-por-contrato]].
  - Problema: inquilinos antiguos pagan menos que el precio de lista y el sistema usaba siempre `room_types.price` → finanzas infladas y portal del inquilino con renta equivocada.
  - Migración aditiva `supabase/migrations/2026-06-12_monthly-rent.sql`: columna `monthly_rent numeric(10,2)` + backfill del precio de lista a los contratos existentes. Idempotente.
  - UI: campo "Renta mensual" en crear (`ContractDialog`, prellenado con el precio de lista) y en ver/editar (`ContractInfoDialog`, con `listPrice` pasado desde `RoomGrid`).
  - Consumidores actualizados al patrón `monthly_rent ?? room_type.price ?? 0`: `FinancesPanel` (ingresos fijos), `app/tenant/dashboard` (renta del inquilino), PDF del contrato.
  - **Fix de paso**: el route del PDF (`app/api/contracts/[contractId]/pdf/route.ts`) leía `rooms.price` (columna inexistente) y siempre daba 404 → corregido a `room_types(price)`; además se limpió el manejo de relaciones array/objeto con un helper `unwrap()` (quita los `as any` que fallaban lint).

## Estado al cerrar
- Código implementado. `tsc --noEmit` limpio. Lint: los 5 errores restantes son `set-state-in-effect` preexistentes (repo-wide); mi código no añade ninguno.
- **Migración `2026-06-12_monthly-rent.sql` NO aplicada aún** en prod — correr en el SQL Editor antes de probar/desplegar (la UI ya consulta `monthly_rent`).
- Sin commit — pendiente de autorización.

## Pendiente / próximos pasos
- Aplicar la migración y verificar backfill (`select monthly_rent from contracts limit 5`).
- E2E: ver contrato existente → renta visible; bajarla a 1,200 → Finanzas suma 1,200 (no el precio de lista) y el portal del inquilino muestra Q1,200; crear contrato nuevo con renta distinta; abrir el PDF (`/api/contracts/{id}/pdf`, ya no debe dar 404).
- Confirmar precios de lista intactos en el sitio público.
- Commit / PR cuando el usuario autorice.
