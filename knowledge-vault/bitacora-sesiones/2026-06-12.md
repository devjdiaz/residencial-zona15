---
tags: [bitacora]
fecha: 2026-06-12
---

# Sesión 2026-06-12

Volver a [[00-Indice]].

## Qué se hizo
- 📄✉️ **Ciclo de contrato por WhatsApp + email del inquilino** (rama `feature/contrato-whatsapp-email`). Decisión: [[email-inquilino-y-contrato-firmado]].
  1. **Plantilla de contrato**: el admin sube un PDF/Word al bucket nuevo `contract-templates` (botón "📄 Plantilla de contrato" en `/admin/rooms`). Al crear un contrato, `CredentialsDialog` ofrece "Enviar plantilla por WhatsApp" con el link público de descarga.
  2. **Email visible**: `tenant_profiles.email` (migración aditiva con backfill desde `auth.users`); se muestra en "Ver contrato" y se guarda al crear contratos nuevos.
  3. **Email editable**: campo en modo edición de `ContractInfoDialog`. Nueva API `/api/admin/update-tenant-email` actualiza `auth.users` (el nuevo email pasa a ser la credencial de login al instante) + el perfil. Si falla (duplicado/inválido), se aborta el guardado completo.
  4. **Contrato firmado + credenciales**: checkbox "Contrato firmado recibido" (`contracts.signed_at`); al marcarlo aparece "📲 Enviar credenciales por WhatsApp" (genera contraseña nueva vía reset existente y abre wa.me con portal/email/contraseña).
  - Fix de paso: `copyCredentials` en `ContractInfoDialog` ponía "(preguntar al admin)" como usuario → ahora usa `tenant.email`, y la URL del portal usa `window.location.origin`.
  - Todo con `logAudit` (plantilla, email, firmado, envíos).
- Migración: `supabase/migrations/2026-06-12_email-signed-template.sql` (carpeta nueva; `schema.sql` intacto). Idempotente, atómica.

## Estado al cerrar
- Código implementado en `feature/contrato-whatsapp-email`. `tsc --noEmit` limpio; errores de lint restantes son preexistentes (en main).
- Smoke test OK contra dev server local (ruta nueva responde 401 sin sesión).
- **Migración SQL NO aplicada aún** — debe correrse en el SQL Editor de Supabase ANTES de mergear/desplegar (la UI nueva consulta columnas/bucket que aún no existen en prod).
- Sin commit — pendiente de autorización.

## Pendiente / próximos pasos
- Aplicar la migración en Supabase (SQL Editor) y verificar backfill.
- Pruebas manuales E2E con datos de prueba (crear contrato, enviar plantilla, editar email, login con email nuevo, marcar firmado, enviar credenciales) y limpiar datos dummy.
- Subir la plantilla real del contrato del cliente.
- Commit / PR cuando el usuario autorice.

---

# Sesión 2026-06-12 (2) — Módulo Historial

## Qué se hizo
- 🗂️ **Módulo Historial** (`/admin/historial`, rama `feature/historial-module`). Decisión: [[2026-06-12-historial-y-archivo-contrato]].
  - Tabs por propiedad + una fila por habitación con 3 botones en modales: **Contrato** (subir/ver/reemplazar el archivo firmado), **Comprobantes** (cronológico mes a mes con Ver/Aceptar/Rechazar) y **Reportes** (`ReportsManager` con prop nueva `roomId`).
  - **Se quitó la sección Comprobantes de Finanzas** — la verificación vive ahora en Historial (KPIs de Finanzas no dependían de `payment_receipts`, verificado).
  - `ContractFileManager` compartido: también montado en "Ver contrato" de Habitaciones.
  - Migración aditiva: `supabase/migrations/2026-06-12_historial-contract-file.sql` (columna `contracts.contract_file_path` + bucket privado `contracts` con policies `current_user_role()`).
- 🧹 **Orden de git**: el trabajo de la sesión 1 (email/WhatsApp) estaba sin commitear y mezclado en el working tree; se separó en su propio commit/PR (PR #7, rama `feature/contrato-whatsapp-email`) — incluyó un fix de compilación en `FinancesPanel` (faltaba `email: ""` en un fallback de `TenantProfile`). El PR de Historial va encima (base = esa rama).

## Estado al cerrar
- PR #7 (email/WhatsApp) y PR de Historial creados, en orden. `tsc --noEmit` y `next build` limpios; errores de lint restantes son preexistentes (patrón `set-state-in-effect` repo-wide).
- **Ninguna de las 2 migraciones aplicada aún en prod** — correr ambas en el SQL Editor (primero email, luego historial) ANTES de mergear/desplegar.

## Pendiente / próximos pasos
- Aplicar ambas migraciones y verificar policies (`select policyname from pg_policies where schemaname='storage'`).
- E2E manual: subir contrato firmado (con usuario `admin`, no super_admin, para validar RLS del bucket nuevo), aceptar/rechazar comprobante desde Historial, confirmar que el tenant dashboard sigue subiendo recibos.
- Mergear PR #7 y luego el de Historial.
